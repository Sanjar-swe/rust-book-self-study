<!-- 1. Переменные и неизменяемость -->

<!-- В Rust все переменные по умолчанию неизменяемы. То есть, если ты создаешь переменную с помощью ключевого слова let, она будет неизменной (immutable), и попытка изменить ее значение приведет к ошибке компиляции. -->

<!-- Чтобы переменная стала изменяемой (mutable), нужно использовать ключевое слово mut: -->

<!-- 2. Типы данных

В Rust существует несколько основных типов данных:

    Целочисленные: i32, u32, i64, u64 и т. д.

    Числа с плавающей точкой: f32, f64

    Булевы значения: bool

    Символы: char -->
